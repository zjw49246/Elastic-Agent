# Elastic-Agent — 项目指南

> **重要：Claude 必须自主维护本文件。** 架构或约定变化时更新，保持简洁。

## 架构要点

- **任务执行两条路径**（worker/runtime.py `_handle_execute`）：
  - subprocess（默认）：Manager 经 AgentType 构造 `claude -p ... --output-format stream-json` 命令行，worker spawn 后逐行转发 stdout
  - **PTY 模式**（可选）：`ExecuteMessage.agent_params` 非空且 worker 装了 [claude-pty](https://github.com/zjw49246/Claude-Code-PTY) 时，worker 用 `ElasticPTYBackend`（worker/pty_backend.py，继承 claude_pty 的 BasePTYBackend）把 Claude Code 宿主在持久 PTY 会话里；`command` 始终随消息下发作为 fallback
- **PTY 事件回传**：带 raw_json 的事件原行透传为 stdout NDJSON（Manager 解析链不变）；交互模式 JSONL 无 result 行，worker 在 turn 结束合成一条（带 session_id，无 cost_usd）；限流/错误 turn → 非零 exit code → 既有凭证轮换照常触发
- **orphan / autonomous 守卫**（pty_backend.py `on_event`）：claude-pty 的 PTYEvent 带 `orphan`（冷恢复时重放的上一 turn JSONL）/ `autonomous`（后台子 agent turn）标记。二者都**不参与前台 turn 的记账**——不设 session_id、不标 turn-fatal 错误、不计入 `_saw_result`/`_saw_claude_output`（`turn_scoped` 门）；orphan 事件还直接从转发流丢弃（避免 Manager 重复落盘 / 把旧 result 当本 turn 结果）。否则 resume 会把旧 api_error 重新标致命 → 刚成功的 turn 被误报 failed（recover-then-failed，借鉴 CCM task #729/#87）
- **瞬时过载同号重试**（P2，`worker/pty_backend.py`）：Anthropic 基础设施侧的临时 429/过载（`overloaded_error` / "temporarily limiting requests (not your usage limit)"）**换号无用**。`core/rate_limit.py` 的 `is_transient_overload`（与 `is_rate_limited`/`is_auth_failure` 互斥、后者优先）识别后，`classify_turn_error` 归为 `transient_overload`；on_exit 不报失败，而是退避（指数+jitter，`transient_retry_delay`）后**同 session `--resume` 重试**（`_schedule_transient_retry`/`_run_transient_retry`，最多 `transient_retry_max=5` 次），耗尽才真失败。额度用尽/认证失败仍走既有 QuotaMonitor 轮换。`rate_limit_event_is_actionable` 备用（避免被 `status=allowed` 例行 ping 误触发轮换）
- **开关**：Manager 侧 `TaskRouter(use_pty=True)` + bootstrap `include_pty=True`；worker 侧无需配置
- **凭证轮换**：原地换凭证（同 config_dir 写新 token）；`CREDENTIAL_LOGIN` 后 worker 调 `recycle_config_dir` 回收该 config_dir 的所有 PTY 会话（温热会话仍持旧账号，不可热复用），下次 EXECUTE 冷恢复读新凭证。recycle 失败会恢复 0600 旧凭据并返回失败；回滚无法验证时本进程 tombstone 该 config_dir，拒绝后续 PTY/subprocess EXECUTE，直到一次完整成功的重新配置或 runtime 重启（重启也同时销毁全部温热 PTY）
- **OAuth token 刷新无 aiohttp 依赖**（`core/claude_oauth.py` `refresh_access_token`）：未注入 `http_client` 时走 **stdlib urllib**（`_post_form_urllib` + `asyncio.to_thread`），不再 `import aiohttp`——worker 框架环境未声明 aiohttp，旧 fallback 会 `ModuleNotFoundError` 致 QuotaChecker 每次刷 token 崩、额度检测/换号失灵
- **账号资料与秘密边界**（`worker/login/`，Codex 流程参考 CCM）：账号以 `agent_type`（`claude|codex`）区分；Manager 将 email、邮箱查询 token 和 OpenAI password 保存在权限 0600 的账号文件中。Codex 账号必须至少有 `email_token` 或 `password` 之一（可同时配置）；REST 永不回显秘密，只返回 `has_email_token`/`has_password`。`ACCOUNT_LOGIN` 跨机传输必须使用 `wss://`（仅可信测试网可显式设 `ELASTIC_AGENT_ALLOW_INSECURE_ACCOUNT_LOGIN=1`）。
- **Agent API 账号（CloudRouter / ApexRouter）**（`core/agent_api.py` + `core/cloudrouter.py` + `core/apexrouter.py` + `worker/agent_api.py`）：独立 0700/0600 store 保存 write-only Key；默认 registry 按 CloudRouter→Apex 顺序显式注册 provider，未知 provider fail closed。CloudRouter 固定 `console.cloudrouter.online`，可投影 Claude/Codex；其显式 `mode=unrestricted` 表示无消费上限，顶层 `balance=0/remaining=0` 仅展示、不得判耗尽，但显式 status/expiry/quota/rate-limit 仍严格拦截。Apex 固定 `https://api.apexin.ai/v1`，只投影 Codex，使用 `apexrouter` Responses provider，并以 pinned Codex `0.144.6` 请求原生 `models[].slug` catalog。Apex `/usage` 的 `used` 是单 Key、`remaining/limits/concurrency` 是共享 group，必须分开归一化；窗口的 `remaining/limit` 同时显式为 `null` 表示该窗口不限额，混合窗口允许，字段缺失或非对称 `null` fail closed，任一受限共享额度耗尽即不可分配。两类 provider 的 usage 做 60 秒 admission（401/403/已耗尽/已过期/无效 schema 不可用，瞬时未知不得复活 fingerprint-bound、跨重启的 last-known unavailable；runtime hard quota 也持久 bench，但正常成功 probe 可清，invalid-key/model tombstone 需显式 refresh；数值和嵌套展示字段有界 allowlist）。API 账号与 OAuth 账号共享 account_id claim/EIP namespace；OAuth 和任何持久 EIP binding 始终独占，只有 `binding=none` 且无 durable binding 的 Agent API identity 用 claim set/refcount 跨 Worker/Job 共享。自动多槽仍要求同一 Worker 使用不同身份，显式重复 ID 只允许已证明为 unbound Agent API。fresh allocation API-first、不可用时回退 OAuth；显式 native/OAuth id 不受无关 API probe 阻塞，API usage probe 有并发上限和绝对 deadline；可选 `account.model` 在 plan→claim→configure 三处校验具体模型（不改写 Mode B 命令本身）。Manager 只在 WSS 登录边界通过关联 `AGENT_API_CONFIGURE` 将 Key 一次性下发，Worker 将 Key 放私有文件并用 helper 供 CLI 取值；Key 不进 JobSpec、REST、CLI config、env 或 argv。投影 schema v2 增加 byte-exact 0700 launcher，以 `/usr/bin/env -i` 清空环境并固定系统 PATH 后再执行 helper，防止 Job 可写 PATH/PYTHONPATH 劫持取 Key 的 Python。CloudRouter Claude PTY 最终 wrapper 再次清全部官方 auth/provider env 并只加载 user settings（排除 project/local hooks/MCP）；Codex 按 provider 写固定 custom provider，并清除官方、CloudRouter、ApexRouter key/base env。CLI 结构化 provider failure 即使 OS exit 0 也转为 Job failure，多帧错误按 auth>硬限流/timeout>瞬时>generic 保留最可操作根因；Apex 普通 429 与 500/502 是共享网关瞬时错误，保持同 Key，只有明确 quota/out-of-credit 才换号。普通 Worker 必须确认销毁后才释放自己的 claim ref，EIP Worker 沿 durable cleanup；Manager 启动恢复未确认旧 Worker 云终态时暂停所有 API 账号选择/Key 下发（OAuth 仍可用）。EIP preflight 拒绝 `run.env`/`run.secret_env` 的 HTTP(S)/ALL proxy 路由变量，兼容容器 owner 还必须拒绝宿主/adapter proxy，保证 provider 观察到绑定 EIP 而非代理出口。Worker 仅对已验证投影强制导出非秘密 `ELASTIC_AGENT_API_PROJECTION_ROOT`，普通任务会清除此保留变量；容器 owner 必须校验 v2 marker/owner/mode/launcher/helper 后只读同路径挂载单一账号 root 并传递对应 CLI home，Elastic 不透明劫持任意 Docker。API 账号只有在 recovery ready、无 claim/lease/binding 时才可通过 mutation guard + binding transaction 删除；refresh 成功可清进程内 quarantine。Job 代码与 helper/key 同属一个 Unix 用户，因此 API Key 是委托给该 Job 的凭据，只能运行可信代码。
- **Agent API 时限与换号边界**：Provider 单请求总时限 15 秒；自动整池 usage 刷新并发 16/总时限 30 秒，未完成 Key 本轮排除并可回退 OAuth；configure 的 usage→读 Key→Worker ACK 共用 60 秒总时限。显式 native/OAuth id 跳过无关 API probe。非 EIP 动态 API→OAuth 换号只能写 projection 外的 `<source-slot>-rot-N` sibling；无法从精确 `.elastic-agent-api/<provider>/<account>/<agent>` 后缀安全反推 source slot 时 fail closed。
- **worker-local 自动登录**：Claude 沿用 Chrome CDP 登录、`claude auth status` 精确 email 校验和 `claude -p` 预热。Codex 在同一 worker 启动 `codex login`，用 Xvfb 下的系统 Chrome + Playwright 完成 OpenAI OAuth，并保留系统 Chrome 原生 UA（禁止固定伪装成与实际 binary 不一致的旧版本）：有 password 时走密码页，仅有 email token 时切换 OpenAI email-code/one-time-code/login-code 入口并自动取码；页面不提供该入口时明确要求补 password。Claude/Codex 浏览器预算由 `account.login_timeout_seconds` 经 `ACCOUNT_LOGIN` 下发（默认 900 秒、范围 60–1200），Manager 端到端等待 3600 秒，为 OTP、身份校验、smoke test 与清理留余量；超时错误只回传脱敏页面状态。可见 anti-bot challenge 最多等待 120 秒，仍未清除就明确提示核对账号绑定 EIP，不空等完整预算。生成的 `CODEX_HOME/auth.json` 必须是 ChatGPT OAuth、包含 access token，且 id_token email 大小写不敏感地精确匹配所选账号，随后 `codex exec` smoke test 必须成功；其非零输出只在本机有界检查，只有窄匹配证实 hard quota/auth 时才通过 `ACCOUNT_LOGIN_RESULT.failure_kind` 回传非秘密枚举并在 Manager 释放 claim 前 quarantine 精确 OAuth 账号，generic/timeout/transient 不隔离。登录失败、Manager 超时或任务取消会通过关联的 `ACCOUNT_LOGIN_CANCEL` 停止 CLI/浏览器进程组并恢复旧 `auth.json`，worker 完成回滚后才发 `ACCOUNT_LOGIN_CANCELLED`；`ACCOUNT_LOGIN_RESULT.cleanup_complete` 仅在失败且已验证回滚时为 `true`，清理不确定为 `false`，成功为 `null`。普通 Worker 对失败且非显式 `true`（含旧 Worker 缺字段）隔离账号，EIP Worker 由销毁实例提供进程/凭据隔离，但上述已证实账号故障仍进入账号 quarantine。OAuth 凭证始终只留在 worker。单槽空 `config_dir` 由 worker 按实际运行用户解析 `~/.codex`；Codex 多槽/换号必须显式给可写的绝对 `config_dir`，禁止猜 `/root`。
- **Codex OTP / 邮箱边界**：Codex 的 `email_token` 是接码/邮箱查询 token，不是 OpenAI token、OpenAI password 或通用 IMAP 密码；token-only 仍由 OpenAI 发送邮箱 OTP，只是 worker 自动查询并填写。password-only 遇 OTP、自动查询不可用/失败或自动码被拒时，worker 才发 `ACCOUNT_LOGIN_OTP_REQUIRED`。`LoginCoordinator` 用可信 `(worker_id, login_request_id)` + Manager 账号快照校验 challenge，并同时快照 `account_email/job_id/job_name/shard_index` 供 UI 精确定位；多个 Worker 可各有独立 challenge，交叉 Worker/账号事件拒绝，同一 challenge 并发提交只转发一次。Manager 通过 `GET /api/accounts/login-attempts` 暴露非秘密元数据，管理员用 `POST /api/accounts/login-attempts/{login_request_id}/otp` 提交 6 位码，验证码转发后不持久化。邮箱查询前永久把进程内 `httpx`/`httpcore` 日志提升到 WARNING，防止带 query token 的完整请求 URL 落入 worker journal（不做临时恢复，避免并发泄漏竞态）。当前 Codex 取码支持 171mail，以及 163/mail.com/onet/gazeta 域名对应的 MailCatcher 后端；其余域名（包括 139.com）默认走 171mail，没有通用 IMAP。

## 两类任务 & 批量编排

- **两类任务模型**（关键区分）：
  - **Mode A — Elastic 托管 agent**（PTY 路径，上文）：任务=一个 prompt，Elastic 宿主 Claude、逐 turn 记账、逐 turn 换号。
  - **Mode B — 不透明长命令**：任务=一条任意 shell 命令（如 `uv run ai4sci-bench run …`），它自己开 sandbox、自己内部消费账号——Elastic **看不到**里面的 turn。此时 Elastic 只做：装环境（bootstrap）+ worker 本地登号 + 跑命令 + 盯输出 + 收结果 + 弹性缩扩。生产 Worker 把 Mode-B 交给独立 `ea-task-supervisor.service`（`worker/task_supervisor.py`）：它与 `ea-runtime.service` 分属 systemd cgroup，拥有 child/waitpid、POSIX process group、stdin、stdout/stderr 0600 spool、timeout 和稳定 terminal event；runtime 重启会经 0600 Unix socket inventory/adopt 同一 PID、重放 spool/终态，首份 STATUS 在 inventory 完成前不得作为“任务不存在”的证据。0700/0600 descriptor 只保存 task id/PID/start ticks/非秘密调度元数据，不保存 command/env/cwd/API Key；launch payload 只短暂经过私有 socket。普通 STOP/timeout/exhaustion/parent 提前退出都先收掉整个 process group 再发终态；冷中断则由 Manager 先发 group-scoped、non-escalating SIGINT，按 grace 再发 group TERM/KILL。`StopMessage.scope/escalate` 默认仍保持旧的 group+Worker 内升级语义，process scope 必须验证 leader PID 未退出/复用。runtime 自身退出只 detach，不能杀 supervisor task。无人值守且会等 stdin EOF 的 CLI（例如直接跑 `codex exec`）必须在 shell 命令中显式加 `</dev/null`。Agent API provider 的瞬时 429/500/502 只允许 CLI 内部自恢复；CLI 最终失败时，即使配置 `rotation.resume_args`，Elastic 也不自动重放任意外层命令（避免重复副作用）。`rotation.resume_args` 只响应已证实的 auth/hard-quota exhaustion。
- **声明式 JobSpec**（`core/job_spec.py`）：任务即数据，所有外部 section 均 `extra="forbid"`，拼错字段直接 422。`environment.profile` 选择版本化、不可变的通用环境（默认 `ubuntu-agent-v1`；Docker 版 `ubuntu-agent-docker-v1`），Job 增量写在 `setup`/`run`。`setup.commands` 旧字符串列表继续兼容；新 `setup.steps` 支持 `name/command/env/cwd/timeout/retries` 且固定以 Job 用户执行。代码可用 `ref`（branch/tag）并用完整 `resolved_commit` 直接 fetch 精确 immutable checkout，不受 mutable ref 后续前进影响。`run.timeout` 缺省/旧 0 归一为 24h，`ttl_seconds` 默认 48h，二者最大 30 天且 TTL 不得短于 run；可选 `run.resume_command` 是完整、显式的应用级 checkpoint 续跑命令，供 suspended Job 新 attempt 及省略 `run.command` 的手工 checkpoint recovery 使用，不能由外层命令猜测，也不等同于换号用的 `rotation.resume_args`。其余 section：`account`(**agent_type**/mode/per_worker/group/config_dir/**binding/ids**)/`rotation`/`fanout`(workers/shard_by/name_prefix/**instance_type/region/disk_gb/spot**)/`collect`/`completion`。`manager_distribute` 对所有新提交/重提统一拒绝，当前托管账号只能走 `worker_local_login`；启动恢复仅用 teardown-only 兼容视图读取旧 `manager_distribute` Job 的 `setup/collect` 做 final collect，视图不含 run/account，绝不重新执行。运行时 Codex 注入 `CODEX_HOME`、Claude 注入 `CLAUDE_CONFIG_DIR`。模板 `{{shard_index}}`/零填充五位 `{{shard_id}}`/`{{num_shards}}`/`{{hostname}}` 由 Manager 渲染，适用于 run/env 和 `setup.s3_datasets[].uri/dest`；`$(hostname -s)`/`$VAR` 留给 worker shell。**per-job 机器覆盖**经 orchestrator→driver→Manager，但 Job region 当前必须与 Manager provider region 一致。
- **账号来源 admission 与恢复排除**：`account.auth_kind=any|oauth|agent_api` 是 plan/submit、普通/EIP claim、登录、Batch 汇总和 checkpoint recovery 的端到端约束；默认 `any` 保持 API-first→OAuth fallback。`oauth` 自动选择必须在枚举/usage probe Agent API 之前短路，`agent_api` 不得回退 OAuth；显式 ID 也必须匹配来源。`account.exclude_ids` 是最多 100 个、与 `ids` 不重叠的非秘密选择排除集，plan 与普通/EIP allocator 都必须严格跳过。EIP checkpoint+非空 `run.resume_command` 即使不启用原机 rotation 也监测 hard exhaustion：Manager 先把该 OAuth 账号放入进程内 quarantine，随后失败并 final collect；`/jobs/recover` 仅在来源 terminal worker 持久化了精确的 checkpoint-exhaustion 标志时清空显式 `ids`，把本次 terminal `account_id` 累加到 `exclude_ids`，普通 checkpoint failure 不换号。quarantine 不跨 Manager 重启，持久的跨 attempt 边界由 recovery JobSpec 的累积排除提供。checkpoint contract v3 绑定 `auth_kind`，但不绑定 `ids/exclude_ids`；旧 v2 只在来源仍为默认 `any` 时兼容读取。
- **cwd / 运行用户契约**（`JobSpec.resolved_cwd` + `compile_job_setup_steps`）：repo clone 到 `setup.target_dir`（= repo 根），setup 和 run 默认都从这里跑；相对 cwd 是其子目录，绝对路径原样用。系统初始化仍以 root 执行，Job-owned setup 固定降权到 provider 配置的 runtime/SSH 用户；`manager_rsync` 也逐 step 保留 env/timeout/retry 并以同一用户执行。
- **代码分发**（`setup.deliver`）：`setup.repo` 只接受无 HTTP userinfo/query/fragment 的远程 http(s)/ssh/git/scp-style URL，拒绝本地路径。`worker_clone` 仅用于 worker 无需 Manager 凭证即可 clone 的仓库，永不隐式下发 `ELASTIC_AGENT_GIT_TOKEN`；私库必须用 **`manager_rsync`**——`core/code_sync.py` `ManagerCodeSync` 在 Manager 本地 clone（token 只在此、`ensure_clone` 后 scrub 掉），再 `rsync --exclude .git` 到 worker（token/`.git`/remote 都不上 worker），最后 SSH 逐个执行结构化 setup step。repo 为空时 `manager_rsync` 仍会创建 target_dir 并执行 setup。setup 固定以 job 用户跑、不 sudo，必须与 run 同用户/HOME。
- **两条接入路**统一成 `Harness`（`harness/generic.py` `resolve_harness`）：`harness_ref` 空→`GenericJobHarness(spec)`（声明式编译 bootstrap/凭证槽/execute）；有值→导入上传的 `Harness` 子类。Harness 是 Manager 任意代码执行边界，API 上传和 `harness_ref` 默认禁用，只有可信部署显式设 `ELASTIC_AGENT_ENABLE_HARNESS_UPLOAD=1` 才开放；上传先在私有临时文件验证并限制 1 MiB，再按内容 SHA-256 发布不可变路径，同名更新不会覆盖历史 Job 代码；不可信 submitter 只允许声明式 JobSpec。
- **账号→EIP 持久绑定**（`core/account_binding.py` + `core/binding_manager.py`）：稳定身份键是 `account_id`（email 仅快照），持久资源只有 AWS EIP；绑定/租约 journal 原子 fsync 且权限固定为 0600（旧文件加载时收紧）。每个 Job worker 使用独占、持久化的 `AccountLease`（job/slot/generation/instance/worker/state），出现 worker 却没有 instance id 的 journal 直接按损坏 fail closed。`reserve` 原子占用账号并按需分配 EIP，`attach_instance` 拒绝抢占已挂到其他实例的地址（AWS `AllowReassociation=False`），`release` 先在 store 单锁内原子完成调用方 lease/account/job/slot/generation/worker/instance 快照比较 + `RELEASING` intent，intent 后身份字段冻结，再可重试地解绑 EIP→确认终止实例→清理并立即移除 Manager task/Node/WS status→**保留 EIP**；allocator claim 也须精确匹配 claim/owner/account 后才释放。缺失/非 `RELEASED`/任一身份冲突一律保留 Node 与账号 claim。只有显式 `decommission` 才永久释放 EIP。
- **EIP Job 生命周期**（`core/batch_orchestrator.py` + `core/batch_hooks.py`）：普通 Job 仍是 `scale_out(N) → provision → login → run`；`account.binding="eip"` 则反转为 `选择/claim 账号 → reserve EIP lease → 创建临时 EC2 → attach EIP → provision → worker-local fresh login → run → final collect → detach EIP → terminate EC2/root EBS → release lease/account claim`。整单各 shard 的 EIP reservation 并行执行但全部收敛后才创建任何 EC2；任一失败/取消会等待其他云调用结束并回滚成功租约。EIP bootstrap 在任何登录/任务流量前禁用 IPv6，且强制把当前 Manager 正在运行的 `elastic_agent` 包 rsync 到 fresh worker 并从源码启动；此安全路径不接受 `ELASTIC_AGENT_FRAMEWORK_SRC` 降级覆盖，会停旧 runtime、断开现有 socket 并要求新服务重连，不能回退到可能缺 request correlation、精确 email 校验和 warmup 判定的旧 PyPI worker。EIP Job 还拒绝 `run.env` 覆盖 `HOME` 或所选 agent 对应的凭证环境变量（Claude=`CLAUDE_CONFIG_DIR`，Codex=`CODEX_HOME`），显式 config dir 始终由已校验账号槽强制注入。终态先停周期收集，再等待 final collect（默认最多 3 次、总计 7200 秒）；失败会标记 Job failed，但仍销毁收费实例。各失败分支也补偿清理临时实例，EIP 留给下个 Job。当前仅支持 AWS，Job region 必须与 Manager/EIP region 相同，`per_worker=1`，不支持原机 `on_exhaust_restart_resume` 换号；`account.ids` 可显式按 worker 选号（数量须等于 fanout），留空则按 `group` 自动分配。
- **AWS EIP/ENI 权限边界**：创建 EIP worker 时给 primary ENI 同步完整 ownership tags；`DisassociateAddress` 同时要求 EIP 与 ENI 都是 `ManagedBy=elastic-agent`，`ReleaseAddress` 仍只授权 EIP，禁止用无条件 `network-interface/*` 解决解绑权限。
- **AWS 大规模启动节流**（`core/providers/aws.py`）：所有 `AWSProvider` 实例共享进程级 `RunInstances` token bucket（2 req/s、burst 5），只对明确的 `Throttling`/`RequestLimitExceeded`/`EC2ThrottledException` 做有界指数 full-jitter 重试；每次重试复用原 `ClientToken`。配额耗尽与 `InsufficientInstanceCapacity` 不重试，交给调度/运维决策；EC2 客户端连接池固定为 64，避免大批量创建/清理时默认 10 连接反复丢弃。Manager 容量 admission 对刚被 AWS/registry 看见、但调用方尚未释放的 inflight create 以精确实例 ID 去重，避免大批并发启动时把同一实例双算并误报 `instance limit exceeded`。
- **EIP 不等于登录态**：固定 EIP 只稳定出口公网 IP，不保存 `auth.json`、浏览器 profile 或设备指纹；每台新 EC2 仍由 worker 本地重新登录；生成的 Claude/Codex OAuth 凭证都不回传 Manager。AWS 默认每 Region 仅 5 个 EIP 配额且公网 IPv4 按小时收费（空闲/使用中均计费），扩号前需申请配额并核算长期 EIP 成本。
- **Job preflight、耐久性与容量护栏**：`POST /api/jobs/plan` 纯读校验并返回不含 env value 的执行预览（profile/source/setup/run/fanout/results/warnings），不写 spec、不 claim 账号、不调用云；真实 submit/resubmit 在首个副作用前复用同一 preflight，拒绝跨 Manager region、超 provider 最大容量、账号静态不足、普通 Job 误用仍有 durable EIP binding 的身份、无实例角色的 S3 dataset、实例类型不在 `ELASTIC_AGENT_ALLOWED_INSTANCE_TYPES`（默认仅 provider 默认值）或 `workers*ttl` 超 `ELASTIC_AGENT_MAX_JOB_WORKER_HOURS`（默认 1440）。通过后才将 JobSpec 原子落到 0600 的 `specs/<job_id>.json` 并 fsync，journal 记录 `prepared→launching→running→suspending→suspended|failed` 或普通 `succeeded|failed|cancelled`；interrupt 的 Idempotency-Key 只保存 SHA-256，并与 `suspending` intent 在同一次 journal replace 中提交。每次 interrupt action 都在 Manager 全局锁内完整校验所有有界私有 Job journal 来保证跨 Job 唯一性，action sidecar 只是可重建的加速缓存。submit Idempotency-Key 以 spec hash 绑定确定性 job id，submit lock 内先精确比较 live/durable replay，已 launch/terminal 的同 key+spec 不受当前账号/容量/preflight 漂移阻断，只有新提交和仅 `prepared` 恢复重新 preflight。重启对中断 Job 先证明 quiescent、final collect 再销毁遗留实例；只有 cleanup=0 且 checkpoint generation/committed_at 与最新完整 pointer 精确一致才可落 `suspended`。Schema 硬上限：`fanout.workers≤100`、`account.per_worker≤32`、`max_rotations≤100`、`disk_gb≤2048`、run/Job TTL≤30 天、collect interval≤86400 秒；AWS/Aliyun `max_instances` 默认 30。
- **JSON JobBatch**（`core/job_batch.py` + `/api/job-batches`）：manifest schema v1 固定 `batch_id/policy/jobs[{client_id,spec}]`，先用 `/plan` 对每个完整 JobSpec 做纯读 preflight 并聚合 worker、worker-hours、账号槽与实例类型；正式提交必须带 Idempotency-Key，全批校验成功后才原子写 0600 私有 batch journal。队列按逐批 `max_active_jobs`、Manager 全局 Job 上限和 provider `max_instances` worker 权重调度（accepted/dispatching batch fanout + 非终态手工 Job，容量不足严格全局 FIFO），每项用稳定子 Idempotency-Key 复用 `_submit_job_payload`；状态为 `queued→accepted→terminal|error`，Manager 重启加载 journal 后继续 queued 项并核对 accepted Job 终态。它不是云事务：各 Job 独立接受/失败。默认每批 20 项（schema 硬限 100）、总 100 workers/1440 worker-hours、全局 3 个 active Job；`ELASTIC_AGENT_JOB_BATCH_MAX_ACTIVE_JOBS` 的代码硬上限为 300，单 manifest 仍最多 10 active Jobs，需要多个 Batch 共同填充大规模并发。其余限制由 `ELASTIC_AGENT_MAX_JOB_BATCH_*` 在硬边界内配置；请求体硬限 2 MiB。REST 永不返回 manifest、env 值或 `run.secret_env` 引用，复制来的脱敏 placeholder 会被拒绝。
- **BatchOrchestrator**（`core/batch_orchestrator.py`）：两种资源路径均经 `FleetDriver` Protocol 解耦（真实现 `core/manager_fleet_driver.py`）；`on_worker_exhausted`/`on_worker_exit` 驱动普通模式换号与任务完成，EIP 模式禁止在原实例切账号。Worker bring-up 与 periodic collect 使用独立有界 semaphore；周期收集支持稳定哈希 jitter，避免数百 Worker 同时发起 SSH/S3，final collect 不受 periodic semaphore 阻塞。
- **增量收集 & 结果落 S3**（`collect.paths` + `interval_seconds`）：只收显式 paths；空列表是 no-op（Batch UI 默认填 `results`，API/SDK 必须显式给），stdout/stderr 只是日志，不会自动成为 S3 对象。`interval_seconds>0` 在首个完整间隔后周期收集，运行中可下载最近一次已完成快照；`0`（Schema/UI 默认）只做终态收集，下载不会临时触发 Worker 同步。成功/失败/取消都会 awaited final collect。每 shard 独立落 `<prefix>/<job>/workers/shard-00000/<path>/` 并写 `_elastic_agent/collection.json`，避免同名覆盖。AWS worker profile+bucket 时 worker 用实例角色直推；否则 rsync `--safe-links` 回 Manager 后由 `S3ResultUploader` 上传；无桶仅留本地。上传按内容 SHA-256 判变，跳过 symlink/special file；结果 API S3 优先并把完整 `file_count` 与最多 500 条 preview 分开，路径/metadata 响应有 aggregate byte budget。Worker 直推部署可设置 `ELASTIC_AGENT_RESULTS_S3_PERIODIC_ENABLED=false`，只关闭 Manager 对整个历史 `collected/` 的后台镜像扫描，不关闭按 Job awaited/final S3 上传。Local 以 open-fd stat+exact EOF、S3 以 ETag/size 条件读+exact EOF 保证 LIST→GET fail closed；score 本地/S3 共用 500 次、16 MiB、500 条和有限有界标量限制。UI 使用独立的 S3 流式 tar 路由（有界 pipe、gzip level 1），在建 pipe/提交 producer 前取得 admission，提前返回、显示传输字节并支持取消，取消时关闭当前 S3 body；原 strict 路由先完整构建，以便响应头前返回读取错误，其临时 archive 用 response-level `finally`、全局并发/20 GiB 逻辑预算、PAX/tar/gzip 最坏上界和磁盘余量共同限制。run 退出靠 `proc.returncode` 而非等管道 EOF，保证 final collect 可触发。
- **跨实例检查点恢复**（`collect.checkpoint` + `recovery`）：checkpoint 要求结果桶、非空且互不重叠的目录、`fanout.shard_by="shard_index"`，并拒绝 hostname 派生的 run/setup/rotation/dataset 值。Manager 以 SHA-256 content-addressed blob→不可变 shard manifest→全 shard Job set 的顺序提交；latest 只在完整 set 间选择，`checkpoint_keep_generations` 默认 3。恢复 source 必须终态或由无 Worker/实例/launch intent/lease 证明 quiescent，source/target 的 fanout/repo/resolved commit/agent/model/paths 必须精确一致。`POST /api/jobs/recover` 从私有 source journal 复制完整 JobSpec，只允许覆盖 generation、run command/timeout 与 TTL；`POST /api/jobs/{id}/interrupt` 只对 live checkpoint+paths+`run.resume_command` 开放，先 durable intent 再 group SIGINT→TERM→KILL、host writer quiescence、final set 和资源释放；无新完整 set 可退回中断前的旧完整 set，无任何 complete set 则 failed。`POST /api/jobs/{id}/resume` 只接受 suspended journal 的 exact generation，复制私有 spec、把 `run.resume_command` 置为新 attempt 的 run command，并记录 direct/root/attempt lineage；UI 不重建 secret/env。preflight 解析真实 S3 set、校验 metadata/精确 shard map/aggregate budget，并在 0600 durable prepare 前把 latest 钉成 exact generation，S3 `COMMITTED` 是权威。所有 shard 在云创建前完成 Manager 逻辑字节+每对象 allocation block+inode 有界下载与 hash/size 验证；每个 rsync 在 spawn 前先 fsync transfer journal，只有整组进程确认消失才删 staging/释放 reservation，否则跨取消/重启 quarantine。provision 后先以 sudo rsync 到 checkout 外 root 私有、同文件系统的 `/var/lib/elastic-agent/recovery-transactions-v1`，Worker fsync+重测总量并 durable 写 `installing`，逐目录同盘 rename 后只有 `installed` marker 才允许 login/dispatch。Manager 崩在目录间时 startup 只前滚，传输未提交则禁止 collect 半状态；启动收集在后台 recovery barrier 内执行且 admission fail-closed，还会禁用 Job 用户 linger/user service、stop+runtime-mask 当前/旧 framework/task units、删除专用 Worker 全部 Docker 容器、停止 Docker/containerd、验证 cgroup 并扫描残留 Job 用户/工作目录进程，然后 reconcile→collect。Manager relay checkpoint 只有在 C locale stderr 可证实纯 vanished-source 的 rc=24 时才有限重传，且必须有后续 rc=0 才发布；失败/部分 rsync 保留上一份完整结果。runtime/WS 重连沿 supervisor 保留原 PID；实例/supervisor 丢失或人工 suspend 都从最后完整 set 新建 Job，应用必须显式 resume，最后 set 后未保存工作重跑。旧 mutable S3 结果只可下载诊断；新 preflight 禁止 `legacy_final_collection`，无 complete set 必须从头开始。
- **人工冷中断与续跑**：`POST /api/jobs/{id}/interrupt` 把 Idempotency-Key 的私有 SHA-256 与 `suspending` intent 一次 fsync 后才发布内存 stop flag/启动后台事务；调用方取消也必须完成该 handoff，同 key 重放复用事务、异 key 冲突。仅 checkpoint+非空 paths+显式 `run.resume_command`、全部预期 shard 已物化且至少一个未确认退出的真实 task 可中断；bootstrap/queued/全 task 已退出或只剩 cleanup 时拒绝，避免改写自然终态。因 `bash -lc`/`uv run` 可能位于应用 coordinator 之上，Manager 先向完整 process group 发送不自动升级的 cooperative SIGINT，再按 grace 发送 group TERM/KILL；这只保证应用有机会原子落盘，未完成 marker 的 active item 续跑时重做。可靠 exit/日志归档后还须停 runtime/supervisor/Docker/containerd 并通过宿主 writer 扫描，才允许 final checkpoint；该证明要求独立的非 root Job/runtime 用户，root 运行用户会 fail closed。S3 complete-set pointer 必须先写本地 journal 才能成为 live latest；普通 Worker 终止后保留含 Job/shard/collection identity 的 `TERMINATED` registry tombstone，EIP 则在 release 前把 collection proof 写入 durable lease，只有 terminal Job journal 成功后才删 tombstone/视为事务完成，覆盖终止后 Manager 崩溃窗口。后台失败做有界同事务重试；shutdown 必须同步收敛已提交 interrupt，否则保留 durable Worker/lease 给下次启动恢复，禁止未证明 quiescent 就收集或释放。全部 shard、final collect、云资源和账号 cleanup 收敛后，只有 journal 中 exact S3 COMMITTED generation 且 `cleanup_pending=0` 才写 `suspended`/`resume_available=true`；final generation 失败可明确警告并钉住旧 complete set，无 complete set 终态为 failed。Manager 重启只接受有效 private intent+summary，并用 lease/tombstone 单调合并每 shard 证明；损坏状态保持 fail-closed。续跑创建新 Job attempt，不复活旧 Job id；journal lineage 由服务端从 source 链推导 `resumed_from_job_id/root_job_id/attempt_no`，同一 source 的并行分支可以拥有相同 attempt_no。
- **外部同步文件读取**（`/api/external/files/...`）：task/path 与 LocalBackend 双层拒绝 traversal；Local reader 逐组件 `openat/O_NOFOLLOW`，S3/OSS 使用带 authoritative length 的 closeable body。manifest 最大 8 MiB/10,000 条且逐字段有界；content 最大 2 GiB、按 256 KiB streaming、验证 exact EOF，response/iterator 断连路径都关闭 reader。全局 HTTP 请求体在 FastAPI/Pydantic 解析前由 `ELASTIC_AGENT_MAX_REQUEST_BODY_BYTES` 限制（默认 16 MiB，允许 64 KiB–64 MiB），chunked 和伪造 Content-Length 同样受限；GET/HEAD/OPTIONS/TRACE 无 body 时不预读。body-bearing 请求使用一次性总读取 deadline（默认 30 秒）、fail-fast 并发 admission（默认 16）和贯穿完整下游 JSON/Pydantic 生命周期的三份 body 聚合预算（默认 256 MiB），所有拒绝、断连、超时、异常和取消路径均先释放预算。
- **可靠终态、取消与清理**：`PROCESS_EXIT`/`RUN_EXHAUSTED` 带 UUID event_id，worker 在 0600 fsync outbox 中按产生顺序保存，Manager 处理成功才 ACK；Mode-B supervisor 在 launch 时预分配稳定 terminal event_id，耗尽也先把 event_id/reason fsync 到非秘密 descriptor，runtime ACK 后才让 supervisor 清除对应 record，因此 runtime 在任意发送边界重启仍按原 id 重放。只有 fsync 成功的事件才进入 pending/send，持久化失败会撤掉 phantom exiting 状态并强制重连，让 Manager 的丢任务恢复接管；ACK 写盘失败则恢复内存真相后重连。重连先 replay 再 STATUS，退出中 task 也进入快照，避免误判。同一 event_id 的 in-flight replay 只由一个 handler 执行，其余等待：成功后去重 ACK，失败/取消则不标 processed，由等待者接手或主动断开 WS 触发 worker 重连重放。普通 LOG 与 file-data 分别使用 frame 数+serialized byte 双上限队列，retry/in-flight 同样占预算；超长物理行持续按 64 KiB byte frame 排水且 UTF-8 跨 frame 安全，完整 raw trace 以本地 NDJSON 为准。终态/control 保持独立 durable/优先通道。若终态 handler 已在清理中移除/替换并关闭当前 WS，Manager 保留 processed event_id 但不向旧连接强发 ACK；短暂重连的 replay 由新连接 ACK，活动连接的真实发送错误仍向上抛出。PTY STOP/launch cancel/shutdown 共用 exactly-once 独立 finalizer，底层 teardown 抛错也必须先 mark exiting 并可靠发终态；Mode B 则先终止整个 POSIX process group。取消会关联取消 bootstrap/login，已 dispatch 的命令先 TERM、超时 KILL、等可靠 exit，再 final collect 和销毁；普通 Job 每个终态 shard 独立执行 final collect→exact cloud termination→registry absence readback→该 Worker claim release，partial failure/replay/shutdown 只重试未完成 shard；EIP Job detach/terminate 后保留 EIP。云创建/registry/WS/collect/终止各失败路径均有幂等后台补偿，Manager shutdown/restart 也扫描带 controller/job tag 的遗留实例。普通 Job 的 create→registry→event 发布与恢复 pass 共用生命周期锁；本进程活实例还须同时匹配内存 instance id、云端 controller/job tag 与 registry metadata，重启不信任旧内存所有权。普通 Job 每次进入云 create 前先在独立 0600 `unbound-launches.json` 按 Job 计数并 fsync；完整 registry/event 发布后才把无 ID 意图转交给 durable NodeRecord，重启会对这些 exact instance id 同样保留完整可见性隔离。无 ID 意图只有匹配实例确认终止或完整窗口连续未发现时才减记/清除。因此 `RunInstances` 已接受但 timeout/cancel 未返回 instance id、Job 已先写 terminal、随后 Manager 再崩溃时，下一进程仍会按 controller/job tag 重扫和销毁幽灵实例。
- **Worker 传输背压**：control / file-data / LOG 三类待发队列均同时限制帧数、单帧序列化大小和总序列化字节，并把 retry/in-flight 帧计入预算。stdout/stderr 的 worker-local NDJSON 是完整权威副本；慢链路时 LOG/file-data 只保留有界最新窗口并按 2 的幂次记录本地丢弃告警，超大 file-data 不升级成 control 事件。control 溢出会强制重连，使 fsync outbox 中的可靠终态先重放。
- **Reconciler 终态守卫**：云 API 会在 EC2 已终止后继续返回历史行；registry 不存在时，terminated **无 lease** orphan 直接忽略。带 lease 的 terminated row 只有在 durable lease 对 lease/instance/account/job 精确匹配，且 `RELEASED` 的 EIP detach、instance terminal readback、必要 worker cleanup、`released_at` 全部提交后才视为历史并忽略；缺 lease、读取异常、非终态、任一身份/完成标记不匹配都继续收养并触发 durable cleanup。raw orphan cleanup 前还必须确认该 instance 未被另一 active lease claim；冲突时 quarantine/保留 registry，禁止 detach/terminate。
- **Job secrets**：`run.secret_env` 只存 `aws-secretsmanager://...` / `aws-ssm://...` 引用，实际值在 dispatch 前即时解析且 API/plan 不回显。跨机传输必须 `wss://`；仅 localhost 或显式 `ELASTIC_AGENT_ALLOW_INSECURE_SECRET_ENV=1` 的可信测试网允许 ws，且不安全传输会在读取 AWS secret 前被拒绝。
- **实盘装配**（`core/batch_hooks.py` `wire_batch`，`Manager.batch` 默认走它）：provision=等实例 running→`BootstrapHandler` SSH 跑 `compile_bootstrap_steps`→等 WS 连上；login=`AccountAllocator`（内存分配 OAuth/API 账号身份，API 候选受 Manager startup-recovery gate 控制，换号时旧号留在本 Job 已用集合；普通 Job 必须先成功销毁 Worker 再 `release_worker` 回池，EIP Job 必须先提交 durable release，bring-up 全失败/rotation 耗尽也走 `_maybe_finish`，释放幂等。若普通 worker 登录取消后 60 秒内未确认清理、或登录中断线，账号进入进程内 quarantine，直到外部确认清理后显式清除；EIP Job 由销毁临时实例提供隔离，不走此 quarantine）+ `LoginCoordinator`（OAuth 发 `ACCOUNT_LOGIN`）/`AgentApiCoordinator`（API 按 registry 验证 provider 后发关联 `AGENT_API_CONFIGURE`，并在读取 Key 前二次检查 recovery gate）；Codex 人工验证码以 `ACCOUNT_LOGIN_OTP_REQUIRED` challenge 关联 request/account/worker，并把 API 提交的 `ACCOUNT_LOGIN_OTP` 精确转发；总等待 3600 秒，断线立即失败，超时/调用方取消会发关联 cancel 并等待清理 ACK。worker 的 `RUN_EXHAUSTED`/`PROCESS_EXIT` 经 event_bus 路由回 orchestrator（`handle_exhausted`/`handle_exit`）；每次 dispatch 同时冻结 `task_id→account_id/auth_kind`，Agent API 认证反馈只 bench 精确旧任务快照，换号后的迟到/重放事件不能误伤新号。manager_url 取 `ELASTIC_AGENT_MANAGER_URL` 或 `config.server`；ssh key 按 provider.type 取。`configure_batch(...)` 可覆盖 hooks。**换号竞态**：中断退出的旧 run 的 `PROCESS_EXIT` 带旧 task_id，`on_worker_exit` 用 task_id 匹配当前 run 丢弃陈旧退出（ROTATING 相位守卫挡不住重派已转 RUNNING 的竞态）。
- **Mode B 换号（策略 a，非 EIP 模式）**：`ExecuteMessage.watch_exhaustion` 开启时 worker `_read_stream` 用 `core/rate_limit.py` 扫 stdout/stderr，撞限流即 `_signal_exhaustion`（发 `RunExhaustedMessage` + SIGINT）；Manager 侧 `on_worker_exhausted` 换号 + 用 `rotation.resume_args` `--resume` 重启，`max_rotations` 上限；打断退出（ROTATING）不算失败。结构化 Agent API auth/hard quota 还会按 immutable task/account snapshot 持久 bench 精确 API 账号；PTY hard quota 终止当前 task，不静默跨号 resume。
- **每机多号（非 EIP 模式）**（`account.per_worker>1`）：GenericJobHarness 按 per_worker 产出多个独立 config_dir 槽（`<base>-slot-N`）；`_bring_up` 逐槽 `driver.login`。自动选择在同一 Worker 内仍给每个槽不同身份，避免“轮换到同一 Key”；无 durable EIP binding 的 Agent API 可同时被其他 Worker/Job refcount 共享，OAuth 始终独占，显式 ids 才可有意重复同一 API identity。换号优先切到**下一个预登录槽**（`active_slot++`，免登录、快），本地池耗尽才 `-rot-N` 现登新号。`WorkerRun.config_dirs/account_ids/account_emails` + `active_slot` 跟踪；`ctx.config_dir` 随活跃槽按 `agent_type` 注入 `CLAUDE_CONFIG_DIR` 或 `CODEX_HOME`。
- **Codex worker bootstrap**：`agent_install_step(agent_type="codex")` 安装固定版本 `@openai/codex@0.144.6`；worker-local 登录依赖系统 Google Chrome、Xvfb 和 Playwright。Codex Job 跳过 Claude 专用的 claude-pty 安装/健康检查，并强制下发当前 Manager worker 源码，避免旧协议把 Codex 请求误作 Claude 登录；source runtime 以 `ELASTIC_AGENT_AGENT_TYPE=codex` 检查/上报 Codex CLI 健康度，不要求机器另装 Claude。
- **Manager 生产启动边界**：AWS 生产入口固定为 `deploy/aws_manager.py` + 版本化 `deploy/aws/elastic-agent-manager.service`。秘密与非秘密云配置分别由必需的 `/etc/elastic-agent-manager.env`、`/etc/elastic-agent-manager.aws.env` 注入；unit 禁用 IMDSv1、屏蔽 env/shared-file/web-identity/container 等替代凭证源，Launcher 用 STS 精确校验专用 Manager role。release/HOME 只读，仅生产 state 目录可写（含 webhook dead-letter），并启用 `NoNewPrivileges`、`PrivateTmp` 和 kernel/control-group 防护；state 必须预建，health `ExecStartPost` 才完成 systemd readiness。AMI/role provenance 失败时在 Manager 生命周期启动前 fail closed；startup 的遗留 Worker quiesce/reconcile/final collect 在后台恢复屏障内执行，不阻塞 systemd 启动超时；`TimeoutStopSec=32400` 覆盖停止时当前 Batch final collect、遗留 EIP wave、普通 orphan wave 与云终态确认的串行最坏上界，避免 systemd 在不可取消的数据保全事务中途强杀。
- **Docker 沙箱支持**（`setup.needs_docker`）：run 命令用 Docker（如 ai4sci-bench `--sandbox os`）时置 true → `compile_bootstrap_steps` 在 runtime 部署**前**插 `docker_install_step`（装 docker.io + `usermod -aG docker <ssh_user>` + `enable --now docker`）。**必须在 runtime systemd 起来前**跑：systemd 起服务时才解析 User 的补充组，usermod 先跑 runtime(及其子 run 命令)才拿得到 docker socket。`run_as` 经 `compile_bootstrap_steps(..., run_as=ssh_user)` 从 `batch_hooks` 传入。
- **Worker 宿主更新边界**：长 Job 期间 Worker 视为不可变。Golden AMI 和每次 bootstrap 都写 `99elastic-agent-no-background-upgrades`，disable+mask `apt-daily*`/`unattended-upgrades.service`，并把 `needrestart` 固定为 list-only、排除 `ea-runtime`/旧 runtime/`ea-task@*` units；所有框架 APT（含 awscli、登录和 Docker 依赖）必须在 runtime 启动前完成。S3 dataset 与 worker-direct collect 只检查预装 `aws`，缺失即 fail closed，严禁在运行期 apt 安装。安全更新通过重建并提升 AMI 完成。
- **Golden worker AMI 快路**：`scripts/build_golden_ami.sh` 从固定 Canonical Ubuntu AMI 构建加密、无凭证的 standard+Docker union image（Docker 默认停用），并覆盖 `ubuntu-agent-docker-sandbox-v1` 所需的 python3-venv/Bubblewrap/util-linux；manifest 必须声明其 dpkg 版本，verifier 确认 `bwrap` 命令存在。Ubuntu 26 的正式隔离能力门是 Job 用户直接运行 bwrap `--unshare-user --unshare-net` probe；util-linux 只保留兼容/诊断用途，不能以外层 `unshare` 成功为前提，且绝不放宽 sysctl/AppArmor。镜像写 `/etc/elastic-agent/image-manifest.json` schema 1 并安装 `/usr/local/bin/elastic-agent-image-verify`。system/agent/login/Docker/runtime Python/固定 commit claude-pty 只有在 manifest 与实际 dpkg/command/CLI/import/direct_url commit **全部精确匹配**时跳过联网安装；marker 缺失/损坏、漂移或验证异常都完整回退既有 apt/npm/pip。镜像构建和重启后验证 background APT units 仍为 masked、needrestart 仍为 report-only。不得 bake OAuth/auth.json、浏览器 profile、AWS 凭证、Job 代码/数据、Manager URL/token、当前框架源码或运行中的 worker unit；详见 `docs/operations/golden-worker-ami.md`。
- **AWS 私网管理面**：Manager 发起的 bootstrap、源码分发、worker-local 登录、日志读取与结果收集统一经 `core/network.py::worker_management_host` 选址；AWS 必须 private_ip-first（EIP 只作 worker 稳定出站身份），其他 provider 保持 public-first，地址缺失时才回落。这样 Worker SG 可仅允许 Manager SG 的 22，不把 SSH 暴露公网。
- **AWS 生产入口与最小权限**：生产 Manager 使用版本控制的 `deploy/aws_manager.py`，部署参数只读 0600 EnvironmentFile，API/Git 秘密无 CLI fallback；启动前 fail-closed 校验 AMI provenance、加密、架构、ENA、IMDSv2 与 golden tags。`deploy/aws/` 保存专用 Manager inline policy、worker 结果写入与 `jobs/datasets/*` 只读 policy 和 cutover/rollback runbook；不得收紧多机共用的旧 `Manager` role，必须给 Elastic Manager 单独 instance profile。per-job 新实例类型或 S3 dataset prefix 必须先显式扩白名单并重跑 Analyzer/simulator。
- **S3 数据集分发**（`setup.s3_datasets: [{uri,dest}]`）：**worker 直连 S3 拉**（不再经 Manager 中转）。前提 `fanout` provider 配了 `worker_instance_profile`（AWS：`AWSProviderConfig.worker_instance_profile`，`create_instance` 给 `run_instances` 加 `IamInstanceProfile`；Manager 角色需 `iam:PassRole`）→ worker EC2 挂 IAM 角色（`elastic-agent-worker`/S3 权限）。provision hook 必须取得该 Worker 的精确 context；多 Worker/含模板时缺 context、未知变量或 hostname/渲染结果为空均 fail closed，不能回退 shard 0 或把单对象退化成整 prefix。模板 token 可包含空白；最终 uri/dest 再做安全校验。awscli 由 runtime 前的 system bootstrap 预装；dataset 阶段只验证 `aws`，缺失直接失败，绝不运行期 apt。随后逐个 `aws s3 sync <uri>/ <dest>`（尾 `/`=前缀递归）或 `aws s3 cp`（单对象）；单对象父目录由 `PurePosixPath(dest).parent` 计算并整体 quote，空格/glob/单引号不拆词。正式 fanout 应优先用 `shard-{{shard_id}}` 单对象，避免每台 worker 重复拉完整 prefix。**GitHub 代码分发不变**（仍 manager_rsync，token 只在 Manager）。旧的 `code_sync.py` `_download_s3`/`ManagerCodeSync.stage_s3`（Manager 下载→rsync）保留为工具函数但 provision 不再用。凭证仍不下发（worker 用实例角色，非静态 key）。
- **Worker / Job 日志**：`GET /api/nodes/{id}/logs?lines=&unit=ea-runtime` 只对存活 Worker 通过 SSH 读取 runtime systemd journal（unit 启动后的登录/运行/协议日志；不含此前 SSH bootstrap 输出）。Job run 的 stdout/stderr 在可靠 `PROCESS_EXIT` 清理实例前由 `JobLogStore` 原子归档到 Manager 私有状态（目录 0700/文件 0600，单 task 尾部最多 5000 条/8 MiB/单条 64 KiB，单 Job 最多 512 task/64 MiB，全局 1 GiB，随 logging retention 默认 30 天和配额淘汰最旧项）；Manager 重启导致内存为空时先从仍存活 Worker 的 `~/ea-logs/<task>.ndjson` 有界回取，取消流程以独立 archive barrier 防止先销毁实例。`GET /api/jobs/{job_id}/logs` 逐 snapshot 流式合并有界 live/archive tail、校验 task 归属且 `no-store`，不能随 Job 总日志量放大内存；Batch UI 对终态执行请求完整的有界 5000 行归档并显示退出码/错误摘要。归档失败释放内存缓冲且不阻止销毁收费实例，旧版已销毁 Job 无法追溯。日志不自动上传 S3，需长期留存时仍应把应用日志写入 `collect.paths`。
- **管理后台账号认证**（`core/management_auth.py` + `api/auth.py` + `api/routes/management_auth.py`）：浏览器以独立管理员邮箱/密码登录，与 Job 的 OAuth/API 账号池完全隔离。用户库只保存 Argon2id hash（目录/文件 `0700/0600`，stdin-only CLI 初始化）；会话使用 `__Host-` Secure/HttpOnly/SameSite=Strict Cookie，CSRF token 只在页面内存，写请求同时校验固定 HTTPS Origin。登录、首次改密与退出共用有界认证/限流/session helper，安全 `next` allowlist 覆盖旧 UI 和 UI v2 深层路由。旧 Bearer service token 只留给自动化；显式错误 Bearer 不得回退 Cookie，Worker `/ws/runtime` token 不经过此模块。
- **前端与绑定管理 API**（`api/routes/`）：`/api/accounts` 返回统一 OAuth/API 账号池；OAuth 密码/邮箱查询 token 与 `/api/agent-api/accounts` 的 CloudRouter/ApexRouter Key 都 write-only，API 只返回存在标志、支持 Agent/模型和缓存额度。Agent API 表单可选择 provider，提交后立即清空 Key；ApexRouter 在 UI/plan 中明确限制为 Codex。API identity 可刷新模型/额度，也可在 recovery ready 且无 claim/lease/binding 时引用感知删除；Job picker 按每个 identity 的实际 Agent 能力过滤。UI 的 non-EIP 选择支持 N 个唯一身份按列表顺序映射，或用一个 unbound Agent API 自动填满全部槽；任意有序/重复映射须直接提交 JobSpec `account.ids`，OAuth/EIP identity 不可重复。Job 提交表单按基本信息/计算资源/代码初始化/账号/运行/结果/换号/Harness 八段组织，保留全部既有控件 ID 与 JobSpec 映射；低频字段折叠，结果路径与增量收集始终显眼，不适用的 Repo/账号/EIP/换号控件会禁用并显示原因，桌面端操作栏保持可达、窄屏改为全宽按钮。提交前前端校验必填命令、数值边界、TTL 与运行超时关系、模板感知的 S3 数据集行格式，并严格拒绝 `run.env`/`run.secret_env` 中缺少 `=`、非法变量名或同框重复的行；AI4Sci Bench 预设使用 `archive/youchengsong-managed-agent-api-20260728`，其他仓库必须改 `setup.ref`。浏览器不接触管理 API Key；pending Idempotency-Key 与冻结 JobSpec 仍只存 sessionStorage。刷新/表单变化后优先提示按原 spec/key 精确重放，丢弃后新建必须双确认。OTP 只在活跃人工 challenge 存在时显示：每个 Worker 在所属 Job 内有独立卡片，明确账号邮箱/ID、完整 Worker、Job 与 shard；新 challenge 首次展开对应 Job，浮动提醒可跳转并在移动端收成小条，多个 challenge 互不遮挡。OTP DOM 以 request+challenge 为 key，Job 轮询替换会移植原节点并恢复输入、焦点和选区；验证码不进入 JS 持久状态或日志。绑定 decommission 双确认且是唯一释放 EIP 入口，active claim/lease 时拒绝；Batch UI 删除有绑定的 OAuth/API identity 时先显示具体 EIP 和不可恢复警告、要求输入完整账号 ID，并在同一 mutation fence 内完成 EIP 释放与 identity 删除；任何 409 都保持未完成状态，若释放后删除失败则明确展示部分成功并隔离身份；账号 CRUD/ensure 与 Job claim 串行。Batch/Fleet 默认浅色、深色仅 session 级可选；Jobs/OTP/Nodes 用 keyed reconcile + 不重叠且隐藏页暂停的轮询，避免全量 DOM 重建、焦点/展开/日志滚动丢失及 results 请求风暴。Results 查询用 per-Job 请求版本拒绝乱序响应，已知非空结果不会被空值/错误回退，终态空结果或查询错误仍有限退避复查；结果操作节点始终保留并区分检查中、暂无、暂不可用、可下载与下载中，单 Job 只允许一个下载请求。每张 Job 卡默认折叠，只在摘要展示状态，用户展开后查看操作、错误、结果和 Worker 明细；轮询替换节点时保留用户的展开、焦点、视口和表格横向滚动。Worker 尚未释放时，即使执行已终态也保留只读 systemd 日志入口，资源消失返回 404/409 后停止自动重试。Batch UI 在 AWS Manager 上默认选择 EIP 模式并展示持久 EIP；显式 `binding=none` 仍受支持，但 plan 会警告它绕过账号 EIP、使用临时公网出口。`/api/jobs` 提供 plan/submit/idempotency/cancel/results/logs，Harness 上传默认关闭。
- **UI v2（/ui-v2 静态壳）**（`api/routes/ui_v2.py` + `api/ui_v2/`）：原生 ES Modules 单页应用（无 Node 运行时依赖），路由 `/ui-v2/{overview,accounts,accounts/new,jobs/new,jobs/batch,jobs,jobs/{id},results,fleet}`；旧 `/|batch` Batch Console 与 `/fleet|dashboard` Fleet Dashboard 保留作回滚面。API 路由先注册，SPA fallback 只在 `/ui-v2/*` 内生效，永不接管 `/api`/`/ws`；JS/CSS 静态资源公开但 shell 要求管理员 session，匿名/首次改密会用 303 保留安全深链接。API client 只发送 same-origin HttpOnly Cookie，非安全方法附加内存 CSRF；启动时清除旧 `ea_api_key`，永不读取/保存/发送管理 Key。password/email token/Agent API Key/OTP/secret_env 不进 store、草稿或日志（`store.js` 的 NEVER_STORE 结构性拒绝）。页面级 poller（`poller.js`）single-flight、hidden 暂停、退避加 jitter、路由切换 abort，全局仅 health(30s)/summary(10s)/OTP(5s)。Job 表单是纯函数模块 `js/core/job-spec.js`（buildJobSpec/validateJobForm 覆盖当前 JobSpec 含 resume_command/checkpoint/recovery），Idempotency intent 按 spec 指纹复用 key；JSON JobBatch 用 `js/core/job-batch.js` 严格拒绝 null/重复 key/超限，plan 与 submit 发送同一份原始 UTF-8，按 `batch_id` 派生稳定 key，并轮询服务端 `job_batch_id` 至终态；EIP 删除仍是 decommission→删 identity 顺序。`GET /api/ui/summary` 只回聚合计数（无 ID/邮箱/Job 名、不扫 S3、5 秒缓存）。`scripts/build_ui_v2.py` 校验 import、内容哈希产出 CDN 包（Cloudflare canary 用，构建产物不入库）；Python 测试 `tests/unit/test_ui_v2.py`，JS 测试 `node --test tests/ui_v2/*.test.mjs`。生产灰度/回滚流程见 `docs/ui-v2-implementation-plan.md`。
- **历史 Job / 配置 / 日志 API 资源边界**：`GET /api/jobs` 的历史部分只用 bounded `scandir`（扫描 10,000、候选 1,000、返回 500、单 journal 32 MiB、总读 64 MiB、历史响应 16 MiB），相关 lease copy 最多 10,000，并返回 `truncated/history_scanned/history_returned`；两线程专用池饱和直接 503。`GET /api/jobs/{job_id}` 用同一有界专用池读取 0600 journal 中不可变的提交时生效 JobSpec，live 内存对象后续变化不能改写历史；current schema 和明确 allowlist 的相邻版本字段先严格投影再脱敏，未知字段/值或损坏 journal fail closed，响应始终 `no-store`。相邻版本兼容只作用于只读详情，当前提交/重提仍严格拒绝，绝不执行未知 schema。`GET /api/jobs/{job_id}/logs` 的归档读取使用四线程专用池和等容量 fail-fast admission，客户端取消必须等真实线程退出后才归还 permit，不能占满共享默认 executor 或堆积已取消扫描。
- **Accounts / Results 状态刷新**：账号占用只有 `run.cleaned_up`/`accounts_released` 才隐藏；Job/lease 读取故障返回脱敏 503，UI 显示未知而非空闲。Accounts 在可见页每 15 秒 single-flight 刷新并提供手动刷新，CRUD/submit/cancel 强制读取会合并且由版本号拒绝乱序。Results 保留已知非空中间快照，但终态 503/空仍有限退避；只有成功非空的终态读取才停止轮询，避免永久冻结旧快照。

## 依赖链（重要）

```
Claude-Code-PTY (claude-pty)  ←  elastic-agent[pty]  ←  下游 harness（audio_book_echo_agent 等）
```

- claude-pty 通过 `[project.optional-dependencies] pty` + `[tool.uv.sources]` 声明，**版本 pin 在 uv.lock**
- **机制保证（优先）**：`scripts/refresh_deps.sh` 自动对比已装 claude-pty commit 与 PTY main HEAD，落后即刷新 lock + 重装——部署/启动流程必须包含它（推荐挂 systemd `ExecStartPre`），`git pull` 本仓库后跑一次即与上游一致
- 手动级联（脚本不可用时）：`uv lock --upgrade-package claude-pty && uv sync`，提交 uv.lock 并 push
- **本仓库更新后**，提醒/级联下游 harness：`uv lock --upgrade-package elastic-agent && uv sync`
- **任务生命周期补充**：领取任务时（步骤 1）先检查上游是否有新版本（`uv lock --upgrade-package claude-pty --dry-run` 或对比 PTY main HEAD 与 lock 中 pin 的 rev）；若本次改动涉及 PTY 接口适配，必须同步 bump lock
- worker 侧 claude-pty 由 bootstrap `pty_install_step` 安装——下游应传入与其 lock 一致的 pinned URL（`pty_package="git+https://github.com/zjw49246/Claude-Code-PTY@<rev>"`）
- 当前 lock/golden worker pin：`7d5a0e5b2192e248e36fc840a5418b773a4560ec`（Session start publication/cancel cleanup 事务化）

## Git 信息

- Remote: https://github.com/zjw49246/Elastic-Agent.git
- 默认分支: main

## 任务生命周期

你收到任务后，按以下 9 步流程自主完成：

1. **领取任务** — 你已被分配任务，阅读本文件和项目代码理解上下文
2. **创建工作区**:
   - `git fetch origin`（如有 remote）
   - `git worktree add -b task-<简短描述> .claude-manager/worktrees/task-<简短描述> origin/main`
   - 进入 worktree 目录工作（后续所有操作在 worktree 中）
   - 如果 worktree 创建失败，直接在当前分支工作
3. **实现功能** — 编写代码，确保可运行
4. **提交代码** — `git add` + `git commit`，commit message 简洁描述改动
5. **Merge + 测试**:
   - `git fetch origin && git merge origin/main`（集成最新代码，如有 remote）
   - 运行测试（如有测试命令）
6. **自动合并到 main**（如有 remote）:
   - `git fetch origin main`
   - `git rebase origin/main`，如果冲突则自行 resolve
   - 如果成功：`git checkout main && git merge <task-branch> && git push origin main`
   - 如果这一步有任何失败，退回到步骤 5 重试
   - （纯本地项目跳过本步）
7. **标记完成** — 更新文档（必须在清理之前，防止进程被杀时状态丢失）
8. **清理** — 回到项目根目录:
   - `git worktree remove .claude-manager/worktrees/<worktree名>`
   - `git branch -D <task-branch>`
   - 如有 remote: `git push origin --delete <task-branch>`
9. **经验沉淀** — 在 PROGRESS.md 记录经验教训（可选）

### 冲突处理

rebase 发生冲突时：
1. 查看冲突文件: `git diff --name-only --diff-filter=U`
2. 逐个解决冲突
3. `git add <resolved-files> && git rebase --continue`
4. 如果无法解决: `git rebase --abort`，退回步骤 5

### 状态判断

- 通过 `git remote -v` 判断是否有 remote
- 有 remote → 必须完成步骤 6（merge + push）
- 无 remote → 跳过步骤 5 的 fetch、步骤 6 和步骤 8 的远程分支删除

## 文件维护规则

> **以下文件都由 Claude Code 自主维护，每次功能变更后必须同步更新。**

- **CLAUDE.md**（本文件）：架构、约定、关键路径变化时更新，只改变化的部分，保持简洁
- **README.md**：面向用户的文档，功能、使用流程变化时同步更新，保持与实际代码一致
- **TEST.md**：测试指南，新增功能时同步添加测试用例和文档
- **PROGRESS.md**：见下方「经验教训沉淀」

## 测试规范

**开发时必须主动使用测试，不是事后补充！**

- **改代码前**：先跑测试，确认基线全绿
- **改代码后**：再跑一遍确认无回归
- **新增功能**：同步新增测试用例，更新 TEST.md
- **修 bug**：先写复现 bug 的测试（红），修复后确认变绿

## 经验教训沉淀

每次遇到问题或完成重要改动后，要在 PROGRESS.md 中记录：
- 遇到了什么问题
- 如何解决的
- 以后如何避免
- **必须附上 git commit ID**

**同样的问题不要犯两次！**

## 注意事项

- 在 worktree 中工作时，不要切换到其他分支
- 完成任务后确保代码可运行、测试通过
